apiVersion: v1
kind: Namespace
metadata:
  name: proxy-local
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: proxy-deployment
  namespace: proxy-local
spec:
  replicas: 1
  selector:
    matchLabels:
      app: proxy
  template:
    metadata:
      labels:
        app: proxy
    spec:
      containers:
        - image: nginx
          imagePullPolicy: Always
          name: nginx
          ports:
            - containerPort: 80
              name: http-server

---
apiVersion: v1
kind: Service
metadata:
  name: proxy-service
  namespace: proxy-local
spec:
  ports:
    - name: http-server
      port: 80
      targetPort: 80

  selector:
    app: proxy
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: proxy
  namespace: proxy-local
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - bolanos.local
      secretName: certificate-local-zh5d
  rules:
    - host: bolanos.local
      http:
        paths:
          - backend:
              service:
                name: proxy-service
                port:
                  number: 80
            path: /
            pathType: Prefix
